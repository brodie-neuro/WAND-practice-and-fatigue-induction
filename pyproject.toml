[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.black]
exclude = '''
/(
    \.venv
  | \.venv_test
)/
'''

[tool.isort]
profile = "black"
skip = [".venv", ".venv_test"]